import React from 'react';
import { IonGrid, IonRow, IonCol } from '@ionic/react';

const All: React.FC = () => {
  return (
    <IonGrid>
      <IonRow>
        <IonCol>
          全部
      </IonCol>
      </IonRow>
    </IonGrid>
  );
};

export default All;
