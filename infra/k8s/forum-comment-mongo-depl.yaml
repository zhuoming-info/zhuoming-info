apiVersion: apps/v1
kind: Deployment
metadata:
  name: forum-comment-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: forum-comment-mongo
  template:
    metadata:
      labels:
        app: forum-comment-mongo
    spec:
      containers:
        - name: forum-comment-mongo
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: forum-comment-mongo-srv
spec:
  selector:
    app: forum-comment-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017
