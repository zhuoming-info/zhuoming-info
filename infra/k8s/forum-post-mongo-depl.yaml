apiVersion: apps/v1
kind: Deployment
metadata:
  name: forum-post-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: forum-post-mongo
  template:
    metadata:
      labels:
        app: forum-post-mongo
    spec:
      containers:
        - name: forum-post-mongo
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: forum-post-mongo-srv
spec:
  selector:
    app: forum-post-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017
