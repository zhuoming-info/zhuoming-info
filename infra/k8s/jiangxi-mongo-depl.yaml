apiVersion: apps/v1
kind: Deployment
metadata:
  name: jiangxi-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: jiangxi-mongo
  template:
    metadata:
      labels:
        app: jiangxi-mongo
    spec:
      containers:
        - name: jiangxi-mongo
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: jiangxi-mongo-srv
spec:
  selector:
    app: jiangxi-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017
