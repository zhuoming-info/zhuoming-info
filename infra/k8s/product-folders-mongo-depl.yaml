apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-folders-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: product-folders-mongo
  template:
    metadata:
      labels:
        app: product-folders-mongo
    spec:
      containers:
        - name: product-folders-mongo
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: product-folders-mongo-srv
spec:
  selector:
    app: product-folders-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017
